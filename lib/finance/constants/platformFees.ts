/**
 * Finance Phase 1 constants — for future use only.
 * Do NOT replace existing fee usages until Finance V2 migration.
 */

/** Default flat platform fee charged on escrow / proposal checkout (USD). */
export const DEFAULT_PLATFORM_FEE_USD = 5;

/** Minimum platform fee for low-value instant purchases (USD). */
export const MIN_PLATFORM_FEE_USD = 1;
